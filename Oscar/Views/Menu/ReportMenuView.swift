import SwiftUI

struct ReportMenuView: View {
    var body: some View {
        Text("Report")
            .font(.largeTitle)
            .foregroundColor(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
} 