import SwiftUI

struct MedicinesMenuView: View {
    var body: some View {
        Text("Manage Medicines")
            .font(.largeTitle)
            .foregroundColor(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
} 