import SwiftUI

struct SettingsMenuView: View {
    var body: some View {
        Text("Settings")
            .font(.largeTitle)
            .foregroundColor(.white)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
} 